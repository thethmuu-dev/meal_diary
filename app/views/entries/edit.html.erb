<% content_for :title do %>
  Edit Entry : <%= @entry.meal_type %>
<% end %>

<div class="columns is-centered">
    <div class="column is-one-third">
        <%= render 'form', entry: @entry %>
    </div>
</div>